interface:
  display_name: "x"
  short_description: "Run the repository architect-to-code loop"
  default_prompt: "Use $x architect to turn this repository goal into root/architect co-creation, an accepted Architecture Brief, Technical Contract, Architect Execution Plan, gated lane sessions, implementation/fix attempts, code reviews, architect integration reviews, lane integration, and a merge-back recommendation."

policy:
  allow_implicit_invocation: true
